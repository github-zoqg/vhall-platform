const globalConfig = {};

// 服务配置
const serverConfig = {
  // 根节点, 组件id规则采用页面名_
  subscribe_layerRoot: {
    component: 'VmpAirContainer',
    children: []
  }
};

export default {
  globalConfig: globalConfig,
  serverConfig: serverConfig
};
