const globalConfig = {};

// 服务配置
const serverConfig = {
  // 根节点
  page1_layerRoot: {
    component: 'VmpAirContainer',
    children: []
  }
};

export default {
  globalConfig: globalConfig,
  serverConfig: serverConfig
};
